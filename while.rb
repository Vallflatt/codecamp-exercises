counter = 1
until counter > 10
    puts counter
    counter = counter + 1
end

counter = 11
while counter <= 20
    puts counter
    counter = counter + 1
end

# use an assignment operator : = sets a variable, counter += 1 : add 1 to counter then assign new value back to counter
# -= and += are used in Ruby, not ++ nor --

